# Tunnelful 0.1.8 设计校验

- 视觉基准：`design/references/current-window.png`（仅作为本地设计参考）
- 浅色实现：`design/actual-app.png`
- 深色实现：`design/actual-app-dark.png`
- 实现截图：2240 × 1560 px，标准 sRGB
- 验证状态：概览页、Tunnel 运行中

## 当前界面

0.1.8 采用六项原生侧栏：概览、Tunnel、发布服务、Ingress 配置、环境检查和日志，当前项使用系统强调色选中。主窗口只保留页面导航与当前页面操作；“关于 Tunnelful”“设置…”和帮助入口位于屏幕顶部的 macOS 系统菜单，避免在窗口工具栏重复出现。主窗口工具栏不再放置隐私遮罩开关。

浅色与深色实现均沿用同一信息结构：本地进程、Cloudflare Edge、源站状态、当前 Tunnel 与运行环境。窗口关闭后应用仍常驻菜单栏，Dock 中不显示额外图标。状态只出现在概览内容里，不再在窗口底栏重复。标题栏与侧栏使用系统材质。

## 对比

参考图与当前浅色、深色实现曾在同一组对比中检查。当前实现保留参考图的克制留白、低饱和中性色、清晰层级与系统控件语义，同时补齐环境检查入口。

公开 PNG 使用示例数据，已转换为标准 sRGB，并移除 EXIF、XMP 与自定义显示器配置。

## 当前结论

- 未发现阻塞 0.1.8 发布的已确认视觉问题。
- 浅色和深色截图均来自当前六项侧栏实现，不再引用旧版窗口。
- 网站完成静态 lint 与生产构建；线上效果仍以 GitHub Pages 部署后的桌面和移动端复核为最终门槛。
- 构建通过不等于真实账户、真实 DNS 或公网链路 UAT；这些操作必须由使用者在自己的 Cloudflare 环境中确认。

当前本地设计检查：通过。
