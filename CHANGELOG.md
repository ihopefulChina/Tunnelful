# 更新日志

本文记录 Tunnelful 面向用户的重要变化。项目仍处于早期阶段，配置格式与功能边界可能继续调整。

## 0.1.9 - 2026-09-04

### 变更

- 外接鼠标时也会隐藏系统传统滚动条，页面仍可滚动，不再露出右侧轨道。
- 更新官网、README 与设计校验中的浅色/深色主窗口截图，去掉截图圆角外的白边。

### 安全与限制

- 更新包仍为 ad-hoc 签名且未公证；Sparkle 使用 EdDSA 校验更新源与磁盘映像。
- 应用仍不捆绑 `cloudflared`，不会读取或上传凭据内容。

## 0.1.8 - 2026-09-04

### 变更

- 主窗口改回系统侧栏与标题栏材质，不再刷自定义窗口底色。
- 去掉与内容重复的标题副标题和窗口底栏；滚动条改为可滚但不显示。
- 更新官网、README 与设计校验中的浅色/深色主窗口截图。

### 安全与限制

- 更新包仍为 ad-hoc 签名且未公证；Sparkle 使用 EdDSA 校验更新源与磁盘映像。
- 应用仍不捆绑 `cloudflared`，不会读取或上传凭据内容。

## 0.1.7 - 2026-09-04

### 变更

- 整理主窗口视觉：工作区与卡片分层、状态点与语义色、窄窗口时状态卡可纵向排列。
- 补齐常用交互与可达性：发布表单可见标签与校验提示、截断文本悬停显示全文、刷新按钮保持宽度、清空日志需确认。
- 更新官网、README 与设计校验中的浅色/深色主窗口截图。

### 安全与限制

- 更新包仍为 ad-hoc 签名且未公证；Sparkle 使用 EdDSA 校验更新源与磁盘映像。
- 应用仍不捆绑 `cloudflared`，不会读取或上传凭据内容。

## 0.1.6 - 2026-09-04

### 变更

- 修复强制使用 HTTP/2 时，TCP 7844 被拦截会把隧道标成“Cloudflare Edge 连不上”，即使 QUIC（UDP 7844）仍然可用。命令行默认走 QUIC，因此能连上。
- HTTP/2 TLS 握手失败时，概览提供“改用 QUIC 重试”；QUIC 被拦截时仍可改用 HTTP/2。
- 预检发现 TCP 7844 不可用、但 QUIC 仍在连接时，不再把整条隧道标红。
- 启动日志与运行环境会显示当前传输协议。
- 整理主窗口侧栏分组、状态卡片与页面间距。

### 安全与限制

- 更新包仍为 ad-hoc 签名且未公证；Sparkle 使用 EdDSA 校验更新源与磁盘映像。
- 应用仍不捆绑 `cloudflared`，不会读取或上传凭据内容。

## 0.1.5 - 2026-09-04

### 变更

- 修复“检查更新…”仍提示 0.1.2：0.1.3 与 0.1.4 的发布流程未生成 Sparkle 更新源，GitHub Pages 一直提供过期的 0.1.2 条目。
- 正式发布现在必须生成、校验并上传 `appcast.xml`；官网部署会改用最新 GitHub Release 中的更新源。
- 应用内更新会忽略不高于当前已安装版本的条目，避免把过期的 0.1.2 当成可安装更新。

### 安全与限制

- 更新包仍为 ad-hoc 签名且未公证；Sparkle 使用 EdDSA 校验更新源与磁盘映像。
- 应用仍不捆绑 `cloudflared`，不会读取或上传凭据内容。

## 0.1.4 - 2026-09-03

### 变更

- 关掉主窗口后，再次打开应用会重新显示主窗口，而不是只留在菜单栏里没有界面。
- 登录项或菜单栏启动时也会做环境检查并按设置启动 Tunnel，不再依赖主窗口已经打开。
- 菜单栏图标区分已停止、正在连接、已连接和无法连接。
- 单条没有 `connIndex` 的 Edge 失败日志不再把仍注册的 HA 连接全部清掉。
- “检查更新…”在 Sparkle 就绪后会正确变为可点。

### 安全与限制

- 更新包仍为 ad-hoc 签名且未公证；Sparkle 使用 EdDSA 校验更新源与磁盘映像。
- 应用仍不捆绑 `cloudflared`，不会读取或上传凭据内容。

## 0.1.3 - 2026-09-03

### 变更

- 修复 0.1.2 无法启动：主程序未写入 Sparkle 的 rpath，且 Hardened Runtime 拒绝加载嵌入的 Sparkle。打开应用会立即被 dyld 终止。
- 0.1.2 无法运行，因此不能通过应用内更新升到 0.1.3。请重新下载安装包。

### 安全与限制

- 更新包仍为 ad-hoc 签名且未公证；Sparkle 使用 EdDSA 校验更新源与磁盘映像。
- 为加载与应用不同签名身份的 Sparkle，发布包启用 `disable-library-validation`。
- 应用仍不捆绑 `cloudflared`，不会读取或上传凭据内容。

## 0.1.2 - 2026-09-03

### 变更

- 软件更新改为 Sparkle。应用内检查、下载并安装官方磁盘映像，不再打开单独的 GitHub 下载页。
- 设置中可开关自动检查更新。
- 若 cloudflared 未能注册任何 Edge 连接，状态显示为“无法连接”，并说明 QUIC 或 HTTP/2 失败原因。
- 设置中可选择 Edge 传输协议；QUIC 被拦截时可改用 HTTP/2 立即重试。

### 安全与限制

- 更新包仍为 ad-hoc 签名且未公证；Sparkle 使用 EdDSA 校验更新源与磁盘映像。
- 应用仍不捆绑 `cloudflared`，不会读取或上传凭据内容。

## 0.1.1 - 2026-09-03

### 变更

- 移除截图隐私模式。界面、DNS 命令、Ingress 编辑和 YAML 预览直接显示真实内容；日志仍会遮罩常见令牌与用户主目录。
- 修复官网“配置留在本机”区块在宽屏下右侧说明被压成竖排单字，并避免主标题在窄屏把页面撑出视口。
- 登录项不再把所有 `notFound` 都提示成“请移到应用程序”。从安装盘或临时副本运行时给出对应说明；应用已在“应用程序”中时允许注册，系统接口不可用时改用用户 LaunchAgent。
- 修正登录项 LaunchAgent 的 `launchctl bootout` 目标格式。
- Edge 状态按 `connIndex` 统计仍注册的连接。单条 HA 连接重连或日志里的 `connection error` 不再把整条隧道标成异常。
- 发布页不再预填 `preview.example.com`；离开该页不再丢掉已生成的 DNS 计划。
- 源站预检将 HTTP 5xx 视为不可达，并在请求结束后释放 URLSession。

### 安全与限制

- 应用仍不捆绑 `cloudflared`，不会读取或上传凭据内容。
- DNS 远端变更必须由用户单独确认，且不会自动覆盖同名的已有记录。
- 当前安装包仍为 ad-hoc 签名，尚未使用 Developer ID 签名或经过 Apple 公证。

## 0.1.0 - 2026-09-03

### 新增

- 增加首次设置与环境检查，可检测 `cloudflared`、Homebrew、本地配置、Tunnel 凭据和账户状态。
- 增加官方 `cloudflared tunnel login` 引导；登录由 Cloudflare 官方网页完成。
- 增加手动检查软件更新入口，只在用户主动操作时访问 GitHub 发布信息。
- 增加明确可见的设置、关于、软件更新与环境检查入口。
- 增加登录时启动应用以及可选的应用启动后运行当前 Tunnel 设置。
- 增加标准 macOS 系统菜单；窗口关闭后自动隐藏 Dock 图标并继续常驻菜单栏。
- 增加确认后执行官方 DNS 路由命令；执行前提供可检查的命令，也可只复制而不运行。
- 隔离可能暗中改变 `cloudflared` 行为的环境变量，并显式关闭 DNS 覆盖。
- 分别提供 Apple 芯片 `arm64` 与 Intel `x86_64` 单架构安装包。

### 变更

- 外观选择会立即应用到全部窗口与菜单栏界面。
- 改进 `cloudflared` 安装来源识别，并允许用户手动选择可执行文件。

### 安全与限制

- 应用仍不捆绑 `cloudflared`，不会读取或上传凭据内容。
- DNS 远端变更必须由用户单独确认，且不会自动覆盖同名的已有记录。
- 当前安装包仍为 ad-hoc 签名，尚未使用 Developer ID 签名或经过 Apple 公证。
