# 安全政策

## 支持范围

| 版本 | 安全维护状态 |
| --- | --- |
| `0.1.4` | 当前版本，接受安全报告；社区维护，不承诺固定响应时限 |
| 更早版本 | 不支持 |

Tunnelful `0.1.4` 使用正式版本号，但当前安装包仍采用 ad-hoc 签名且尚未经过 Apple 公证。它不应被视为生产环境的安全边界，也不适合在无人值守的关键主机上部署。

## 信任边界

Tunnelful 是官方 `cloudflared` 的 macOS 控制面，不实现 Cloudflare Tunnel 协议。应用负责本地配置、进程管理、源站预检和状态展示；认证、Tunnel 连接与流量转发由用户安装的官方 `cloudflared` 完成。

使用 Tunnelful 表示你信任以下本地输入：

- 当前用户可执行的 `cloudflared` 文件。
- 由你选择或放置在常见配置目录中的 YAML 文件。
- YAML 引用的 Tunnel credentials 文件。
- 你输入并要求应用检查的 HTTP/HTTPS 源站地址。

不要导入不可信配置，不要选择来源不明或可被其他用户替换的可执行文件。

## 当前安全措施

- 使用 `Process` 参数数组启动命令，不经 shell 解释。
- 只停止或重启由当前 Tunnelful 进程创建的子进程。
- 保存配置前执行本地检查与官方 CLI 校验。
- DNS 路由命令会先提供预览；执行前由用户核对真实目标并单独确认。命令显式传入 `--overwrite-dns=false`。
- DNS 命令超时或应用在执行中退出时，远端结果可能未知；重试前必须先在 Cloudflare DNS 核对记录。
- 启动 `cloudflared` 时移除 `TUNNEL_*`、`CF_*` 与 `CLOUDFLARED_*` 环境变量，避免宿主环境静默切换账户、Token 或远端操作语义。
- 配置采用原子写入；覆盖前备份，原文件权限存在时予以保持，新文件默认使用仅当前用户可读写的权限。
- 临时预览配置使用仅当前用户可读写的权限，并在操作结束后删除。
- 日志会遮罩常见 Bearer 值、令牌参数、JWT 形态文本和用户主目录。
- 项目代码不加入遥测、广告或分析 SDK。

## 已知限制

- 日志脱敏基于模式匹配，不能保证识别任意秘密格式。导出、复制或截图前必须人工检查。
- YAML 的未知字段会尽量保留，这也意味着恶意或错误配置仍可能被传递给官方 CLI。
- 应用当前未启用 App Sandbox，以便读取现有本地配置并启动外部 `cloudflared`；它以当前登录用户权限运行。
- 应用不会验证外部 `cloudflared` 文件的发布者签名或摘要。用户必须自行确认安装来源。
- Edge 连接状态来自进程输出，不等于端到端可用性或 Access 策略正确性。
- HTTP/HTTPS 源站预检会真实发送 GET 请求，不应将具有副作用的地址用于预检。
- 配置备份可能包含与原文件相同的敏感路径和参数，必须限制其读取权限和分享范围。
- 当前发布包未经过 Developer ID 签名和 Apple 公证，无法提供正式发布版的来源保证。

## 凭据处理建议

- 不要把 Tunnel credentials、API 凭据或 Access 凭据提交到版本库。
- 不要在问题、日志、截图、录屏或 DNS 命令中粘贴令牌。
- 让 credentials 文件及其父目录仅对当前用户可读。
- 分享 YAML 前删除 Tunnel 标识、credentials 路径、真实域名与源站地址。
- 若怀疑凭据泄漏，请在 Cloudflare 控制面轮换或撤销相应凭据，并停止受影响的连接器。

## 报告安全问题

请优先使用代码托管平台提供的私密安全报告功能。报告中应包含：

- 受影响版本与 macOS 版本。
- 问题类型、可利用条件和可能影响。
- 不含真实凭据、域名或个人路径的最小复现步骤。
- 如有修复建议，可附上补丁或伪代码。

不要在公开问题中提交未修复漏洞、真实凭据、完整日志或可识别用户的信息。如果当前托管平台未启用私密报告，请只创建一个不含漏洞细节的公开问题，请求维护者建立私密沟通渠道。

## 披露原则

维护者会在能力范围内确认报告、评估影响并准备修复。由于当前是志愿维护的开源项目，不承诺固定响应或修复时限。修复发布前，请避免公开可直接利用的细节。
